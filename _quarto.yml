# Quarto requires this project-root loader. Edit the configuration it includes
# under quarto/ and keep author-owned manuscript metadata under document/.
metadata-files:
  - quarto/project.yml
  - document/metadata.yml
  - document/chapters.yml
